<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= title %></title>
    <link rel="stylesheet" href="/public/stylesheets/style.css" />
  </head>
  <body>
    <nav>
      <ul>
        <li>
          <a href="/">Головна</a>
        </li>
        <li>
          <a href="/contact">Контакт</a>
        </li>
        <li>
          <a href="/blog" class="active">Блог</a>
        </li>
        <li>
          <a href="/weather">Погода</a>
        </li>
      </ul>
    </nav>
    <h1>Blog File</h1>
    <div><%= articles.map(parseArticles) %></div>
  </body>
</html>

<% function parseArticles(article) { %>
<h2><%= article.title %></h2>
<p><%= article.body %></p>
<p><%= article.date %> by <%= article.author %></p>
<% } %>
